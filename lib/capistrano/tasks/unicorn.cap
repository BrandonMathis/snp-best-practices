namespace :deploy do
  task :restart do
    invoke 'unicorn:restart'
  end
end

Capistrano::DSL.stages.each do |stage|
  after stage, 'deploy:set_rails_env' # TODO: remove this line after capistrano-rails update
  after 'deploy:publishing', 'deploy:restart'
end
